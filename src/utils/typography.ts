import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  baseLineHeight: 1.666,
  headerFontFamily: ['MPLUS1p', 'Apple SD Gothic Neo', 'Nanum Gothic'],
  bodyFontFamily: ['MPLUS1p', 'Apple SD Gothic Neo', 'Nanum Gothic'],
});

export default typography;
